# Streamlit Community Cloud (Python 3.12–3.13): use versions with prebuilt wheels.
# Avoid numpy<2 / old pandas on 3.13 — they compile from source and crash-loop the deploy.
streamlit==1.58.0
pandas==2.2.3
scikit-learn==1.5.2
numpy>=2.1.0,<3
joblib==1.4.2
openpyxl>=3.1.0
